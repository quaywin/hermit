diff --git a/lib/hermit/vpn/inbound/tailscale.ex b/lib/hermit/vpn/inbound/tailscale.ex
index 354c380..f8f2b1d 100644
--- a/lib/hermit/vpn/inbound/tailscale.ex
+++ b/lib/hermit/vpn/inbound/tailscale.ex
@@ -118,5 +118,6 @@ defmodule Hermit.Vpn.Inbound.Tailscale do
 
           ts_up_args =
-            if String.trim(advertise_routes) != "" do
-              ts_up_args ++ ["--advertise-routes=#{String.trim(advertise_routes)}"]
+            cleaned_routes = clean_routes(advertise_routes)
+            if cleaned_routes != "" do
+              ts_up_args ++ ["--advertise-routes=#{cleaned_routes}"]
             else
@@ -231,5 +232,6 @@ defmodule Hermit.Vpn.Inbound.Tailscale do
 
         ts_up_args =
-          if String.trim(advertise_routes) != "" do
-            ts_up_args ++ ["--advertise-routes=#{String.trim(advertise_routes)}"]
+          cleaned_routes = clean_routes(advertise_routes)
+          if cleaned_routes != "" do
+            ts_up_args ++ ["--advertise-routes=#{cleaned_routes}"]
           else
@@ -957,2 +959,10 @@ defmodule Hermit.Vpn.Inbound.Tailscale do
 
+  defp clean_routes(nil), do: ""
+  defp clean_routes(routes) when is_binary(routes) do
+    routes
+    |> String.split(",")
+    |> Enum.map(&String.trim/1)
+    |> Enum.reject(&(&1 == ""))
+    |> Enum.join(",")
+  end
+
   defp mock? do
