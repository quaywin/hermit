services:
  hermit:
    image: ghcr.io/quaywin/hermit:latest
    container_name: hermit_orchestrator
    ports:
      - "${HERMIT_PORT:-3000}:3000" # Web dashboard
      - "41641-41700:41641-41700/udp" # Tailscale UDP port range
    # Use Sysbox runtime for advanced security isolation on Linux
    runtime: sysbox-runc
    cap_add:
      - NET_ADMIN
      - SYS_ADMIN
    devices:
      - "/dev/net/tun:/dev/net/tun"
    env_file:
      - ${HOME}/.hermit/env
    volumes:
      # Mount storage directory for Sandboxed configurations
      - ${HOME}/.hermit/storage:/app/storage
      # Mount docker socket for 1-click self-upgrade from Web UI
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - PHX_SERVER=true
      - PORT=3000
      - STORAGE_BASE_PATH=/app/storage
    restart: "unless-stopped"
